{
  "mcpServers": {
    "discord": {
      "command": "sh",
      "args": [
        "-c",
        "set -a && . /YOUR/PROJECT/PATH/.env && set +a && DISCORD_TOKEN=\"$DISCORD_BOT_TOKEN\" npx -y @pasympa/discord-mcp"
      ]
    }
  }
}
